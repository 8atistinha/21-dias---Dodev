//FAzer uma contagem do zero até o numero selecionado pelo usuário

let num1 = parseInt(Number(prompt("escolha um numero positivo e inteiro")));
for(let num2 = 0; num2 <= num1; num2++)
console.log(1*num2);
